# GZ-14 功能点开发日志

> 项目：游戏后台（局外系统）  
> 进度追踪文档，每个功能点完成后标记 ✅

---

## Phase 1：基础组件与网络层

### 1.1 项目骨架
- [x] `go mod init` 创建 Go Module
- [x] 目录结构搭建
- [x] Makefile / 构建脚本

### 1.2 协议定义（Protobuf）
- [x] 定义 Enum：MsgID、ErrCode
- [x] 定义消息体结构（LoginReq/LoginRsp, CreateRoleReq/Rsp, EnterSceneReq/Rsp, MoveReq/MoveBroadcast, Heartbeat, LogoutReq/Rsp, AOIEnter/AOILeave）
- [x] protoc 编译生成 Go 代码

### 1.3 网络层
- [x] Packet：封包（Length + MsgID + Sequence + ErrCode + Body）
- [x] Packet：解包（处理粘包/半包）
- [x] Conn：TCP 连接封装（读写 goroutine、Close 安全）
- [x] Server：TCP Server（Accept、连接管理、消息分发）
- [x] Codec：Protobuf 序列化/反序列化封装

---

## Phase 2：Lobby 服务

### 2.1 基础框架
- [x] TCP Server 启动（监听端口、Accept 循环）
- [x] 连接管理器（ConnID → *Conn, 安全增删）
- [x] 消息分发路由（MsgID → Handler 映射）
- [x] Session 管理（Token → uid, Redis 过期策略）

### 2.2 账号系统
- [x] 注册流程（用户名 + 密码 → 写入 DB）
- [x] 密码加密存储（bcrypt）
- [x] 登录鉴权（验证密码 → 生成 Token → 返回角色列表）

### 2.3 角色管理
- [x] 角色创建（名字校验 + 数量上限 → 写入 DB）
- [x] 角色列表查询（登录后返回角色列表）
- [x] 角色选择（进入场景前 Token + 角色归属校验）

### 2.4 登出与连接管理
- [x] 主动登出流程（清理 Session → 通知 Scene → 位置持久化）
- [x] 心跳响应（Echo 模式）
- [x] 断线重连（Session 保持 → Token 续期 → 场景恢复）
- [x] 异常断开清理（超时 → 强制登出）

---

## Phase 3：Scene 服务

### 3.1 基础框架
- [x] TCP Server 启动（监听端口）
- [x] 场景管理器（MapID → Scene 映射）
- [x] 玩家在场景中的实体管理（EntityManager）

### 3.2 AOI 网格系统
- [x] Grid 数据结构（实体列表、增删查）
- [x] AOI 管理器（网格划分、坐标 → 格子映射）
- [x] 进入场景：计算周围格子 → 推送 AOIEnter 列表
- [x] 离开场景：通知周围格子 → 推送 AOILeave
- [x] 跨格检测：移动时判断格子变化 → 触发 Enter/Leave

### 3.3 移动同步
- [x] 移动请求处理（位置校验 → 更新实体位置）
- [x] 格内移动广播（同格子不触发 AOI 事件，只广播位置）
- [x] 跨格移动广播（触发 AOI Enter/Leave + 位置广播）
- [x] 移动频率限制（单玩家限频 20 次/s）
- [x] 边界限制（坐标 clamp 在地图范围 0~1000）

---

## Phase 4：DB 服务

### 4.1 MySQL 模块
- [x] gorm 模型定义（Account、Role）
- [x] 账号表 CRUD（创建、查询）
- [x] 角色表 CRUD（创建、查询、更新位置）
- [x] 自动建表（AutoMigrate）

### 4.2 Redis 模块
- [x] Session 缓存（SET/GET/DEL + TTL）
- [x] 在线状态管理（SET/GET + 过期）
- [x] 位置缓存（HSET 实时位置）

### 4.3 存储策略
- [x] 写回策略：移动时异步写 Redis，登出时写回 MySQL
- [x] 角色加载：从 MySQL 加载 → 进入场景时缓存到 Redis
- [x] 登出保存：Redis 最新位置写回 MySQL

---

## Phase 5：服务间通信

### 5.1 内部 RPC（接口注入）
- [x] Lobby → Scene：PlayerEnterScene / PlayerLeaveScene 回调
- [x] Scene → DB：通过 Manager 直接访问
- [x] Lobby → DB：通过 Manager 直接访问
- [x] 接口抽象避免循环依赖（SceneAPI / LobbyAPI）

---

## Phase 6：压测工具

### 6.1 Bot 框架
- [x] Bot Manager：控制 N 个机器人生命周期
- [x] Bot Client：模拟客户端行为（注册 → 登录 → 创角 → 进入场景 → 移动 → 登出）
- [x] 启动速率控制（Rate Per Second）
- [x] 随机移动行为（随机方向/步长）

### 6.2 数据采集
- [x] 延迟采集（登录耗时、进入场景耗时）
- [x] 成功率统计（登录成功/失败、进入场景成功/失败）
- [x] 吞吐量统计（发送移动数、接收广播数）
- [x] 延迟百分位统计（Avg / P50 / P95 / P99）
- [x] 控制台报告输出

---

## Phase 7：测试与报告

### 7.1 功能测试
- [x] 协议层测试（封包/解包/粘包）
- [x] AOI 单元测试
- [x] 登录流程集成测试（通过 Bot E2E）
- [x] E2E 场景测试脚本

### 7.2 性能测试
- [x] 千人登录风暴（1000 bots 100% 完成 @20/s，P99 超目标）
- [x] 千人移动广播（200 bots 全量移动验证）
- [x] 反复登录登出（100 bots × 3 轮）
- [x] 混合压力

### 7.3 分析优化
- [x] pprof 火焰图采集
- [x] 性能瓶颈分析（bcrypt 确认核心瓶颈）
- [x] 优化迭代
- [x] 最终测试报告输出

---

## 当前进度总览

| Phase | 功能点数 | 已完成 | 完成率 |
|-------|---------|-------|-------|
| P1 基础组件 | 9 | 9 | 100% |
| P2 Lobby | 11 | 11 | 100% |
| P3 Scene | 11 | 11 | 100% |
| P4 DB | 9 | 9 | 100% |
| P5 服务间通信 | 4 | 4 | 100% |
| P6 压测工具 | 9 | 9 | 100% |
| P7 测试报告 | 8 | 8 | 100% |
| **总计** | **61** | **61** | **100%** |
